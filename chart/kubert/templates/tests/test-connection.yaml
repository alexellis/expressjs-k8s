apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubert.fullname" . }}-test-connection"
  labels:
{{ include "kubert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubert.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
